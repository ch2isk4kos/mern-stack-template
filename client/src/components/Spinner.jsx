import React from "react";

const Spinner = () => {
  return (
    <div id="Spinner" className="loadingSpinnerContainer">
      <div className="loadingSpinner"></div>
    </div>
  );
};

export default Spinner;
